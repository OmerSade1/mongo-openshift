apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/OmerSade1/mongo-openshift.git
    targetRevision: HEAD
    path: frutis
    helm:
      valueFiles:
        - values.yaml 
  destination:
    server: <managed-cluster-name>
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true